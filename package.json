{
  "name": "time-counters",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && esbuild index.js --bundle --format=iife --outfile=dist/app.js --minify --sourcemap && node scripts/prepare-dist.js",
    "dev": "node scripts/prepare-dist.js && esbuild index.js --bundle --format=iife --outfile=dist/app.js --sourcemap --watch"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SaurusLex/time-counters.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/SaurusLex/time-counters/issues"
  },
  "homepage": "https://github.com/SaurusLex/time-counters#readme",
  "devDependencies": {
    "esbuild": "^0.27.3"
  },
  "dependencies": {
    "firebase": "^12.10.0"
  }
}
